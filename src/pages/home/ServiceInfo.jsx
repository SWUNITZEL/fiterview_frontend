import {Button} from "@mui/material";
import {
  Container,
  Dialog,
  DialogTitle,
  DialogContent
} from "@mui/material";

export default function ServiceInfo({onNavigate}){

    return (
        <div>
        </div>
    );
};